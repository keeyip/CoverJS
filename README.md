# > coverjs-gold

#### Use regex to exclude files and folders
`--regex-exclude '^third-party-libs|config-scripts|.bak$'`

#### Opt-in to debug

`--debug-recursing`

`--debug-excludes`

`--debug-instrumenting`

# > coverjs-gold-report
